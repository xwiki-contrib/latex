\documentclass{article}
\usepackage{standalone}

$processor.render('default/Preamble')

\begin{document}

#foreach ($latexPageInclude in $latexPageIncludes)
  \include{$latex.escape($latexPageInclude)}
#end

\end{document}