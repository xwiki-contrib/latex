#set ($format = $block.getFormat().toString())
#if ($format == 'BOLD')\textbf{${processor.process($block.getChildren())}}#elseif ($format == 'ITALIC')\textit{${processor.process($block.getChildren())}}#elseif ($format == 'MONOSPACE')\texttt{${processor.process($block.getChildren())}}#{else}$processor.process($block.getChildren())#end