#set ($discard = $latex.getStack('isInContainerAcceptingStandalone').push(true))


## If there content only has a single TableBlock then use the table environment, otherwise use the figure environment
#if ($latex.isTable($block.children.get(0)))
\begin{table}[h]
$processor.process($block.getChildren())
\end{table}##
#else
\begin{figure}[h]
$processor.process($block.getChildren())
\end{figure}##
#end
#set ($discard = $latex.getStack('isInContainerAcceptingStandalone').pop())