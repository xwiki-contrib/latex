#if (!$latex.block.isInline())


\begin{xwikiwarning}
## We skip the GroupBlock to make the output nicer since it's not needed to output a varwidth env
$latex.tool.escape($latex.processor.process($latex.block.children.get(0).children))
\end{xwikiwarning}##
#else
\xwikiwarninginline{$latex.processor.process($latex.block.getChildren())}##
#end