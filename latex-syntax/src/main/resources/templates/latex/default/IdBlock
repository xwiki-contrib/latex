## Since there's no info if the IdBlock is inline or not, we need to check if it's inside an inline MacroMarkerBlock
## or not...
#if ("$!latex.block.parent.class.simpleName" == 'MacroMarkerBlock')
  #if (!$latex.block.parent.isInline())


  #end
  \label{$latex.tool.normalizeLabel($latex.block.name)}##
#end