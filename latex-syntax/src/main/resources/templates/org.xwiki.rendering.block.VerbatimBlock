## TODO: Need to find a separator not used in text
#if ($block.isInline())
\verb|$block.getProtectedString()|#else
## Since this is a standalone element we separate from the previous block by 2 NL


\begin{verbatim}
$block.getProtectedString()
\end{verbatim}#end