## If we're not on the last row add a hline too.
#set ($currentBlock = $block)
${processor.process($block.getChildren())} \\
#if("$!currentBlock.getNextSibling().class.getSimpleName()" == 'TableRowBlock')
\hline
#end
