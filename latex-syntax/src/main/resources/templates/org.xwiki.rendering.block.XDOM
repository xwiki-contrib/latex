\documentclass{article}

## Used for strike through support
\usepackage[normalem]{ulem}
## Used for image support
\usepackage{graphicx}
## Used for links
\usepackage{hyperref}

\begin{document}$processor.process($block.getChildren())

\end{document}